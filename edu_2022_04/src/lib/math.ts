export function add(x: number, y: number): number{
    return x+y;
}

export function add2(x: number, y: number): number{
    return x+y;
}

export default function multiple(x: number, y: number): number{
    return x+y;
}   // default는 한번만 쓸 수 있음.

var a = 222;